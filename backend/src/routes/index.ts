import IController from '../interfaces/success/controller.interface';

// global routes
export const controllers: IController[] = [
  // { path: '/auth', router: new AuthController().router },
  // {
  //   path: '/users',
  //   router: new UserController().router,
  //   protected: true,
  // },
];
